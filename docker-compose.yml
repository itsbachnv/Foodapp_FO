services:
  web:
    build:
      context: ./mobile
      dockerfile: Dockerfile
      args:
        EXPO_PUBLIC_API_BASE_URL: ${EXPO_PUBLIC_API_BASE_URL:-http://localhost:8080/api/v1}
    container_name: foodapp-fo-web
    restart: unless-stopped
    ports:
      - "3000:80"
